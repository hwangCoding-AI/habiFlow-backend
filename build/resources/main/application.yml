spring:
  r2dbc:
    url: r2dbc:postgresql://localhost:5432/habiflow
    username: postgres
    password: password
  datasource:
    driver-class-name: org.postgresql.Driver
  sql:
    init:
      mode: never
  main:
    web-application-type: reactive
